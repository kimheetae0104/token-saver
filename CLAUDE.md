# 토큰 세이버 — 작업 규칙

> 북극성: **토큰 최소화가 아니라 "토큰당 아웃풋" 극대화.** context rot·장황함≠품질 때문에
> 노이즈를 깎으면 싸지고 *동시에* 좋아진다. 목표 = 신호 밀도. 단 하나의 경계: **필요한 추론은 자르지 마라.**
> 상세 근거·수치는 [docs/TOKEN-GUIDE.md](docs/TOKEN-GUIDE.md). 측정은 `python3 measure.py`.

## 탐색
- 통독 금지. **grep으로 위치 먼저** → Read는 `offset`/`limit`로 필요한 줄만.
- 큰 도구 출력(로그·`ls -R`)은 head/grep/wc로 좁혀서. 그대로 받지 않는다.
- 여러 곳을 훑어야 하면 서브에이전트에 위임하고 **결론만 회수**.

## 위임 (서브에이전트)
- 대량 탐색·검증·파싱은 서브에이전트로. 부모엔 요약만 돌아온다(격리 캐시).
- 서브에이전트는 cold-start → **trivial 작업은 하나로 배치**(item당 1개 금지).
- 라우팅=사다리: Haiku→오라클 검증→실패시 **프롬프트 강화 먼저**→재실패시 Sonnet(의미론 경계)→Opus. 오라클 없고 고위험이면 Sonnet 이상부터 — 단, **오라클 없어도 대규모 반복(N≥50 수준)이고 배치판정을 쓸 수 있으면 사다리가 여전히 유리**(실험9후속2·6, 배치판정 0.35~0.44배, 에스컬레이션률 2.86% [Wilson 95% CI 0.8~9.8%]). 소표본(N<10)에서는 에스컬레이션 1건만 나와도 역전되니 소표본 오라클-없음 과제는 Sonnet 직행.
- effort도 라우팅: 기계적=low, 어려운 추론만 high. **thinking=output 단가(5×)**라 절감 큼.
- **다단계 파이프라인(생성→판정→비용측정 등) 통위임 금지** — 서브에이전트 1개가 내부에서 Bash를
  여러 번 호출하며 스스로 오케스트레이션하면 오버헤드가 절감분을 거의 다 먹는다(실측: 컨텐츠만
  봤을 때 74.1% 절감 → 오버헤드 포함 11.5%, [실험10](experiments/PROTOCOL.md)). 메인이 각 단계를
  **병렬 직접호출**(Agent 여러 콜, 한 메시지에)로 쪼개고 결과만 회수할 것. `measure.py --autopsy`가
  이 패턴을 "위임 오버헤드 의심"으로 사후 감지한다.

## 왕복 최소화
- 독립적인 도구 호출은 **한 메시지에 병렬로**.
- 비사소 요청은 착수 전 **4슬롯 확보**: 의도·제약·성공기준·위임경계. 애매하면 파싱본 echo 후 확인.
- 계획 후 한 번에 실행. 시행착오 반복·불필요한 재확인 금지.
- 확실하면 즉시 실행.

## 캐시 (숨은 최대 절약)
- **세션 시작 시 모델·effort 정하고 유지.** `/model`·`/effort`·`/fast` 전환은 캐시 무효화.
- 안정적 컨텍스트를 앞에 고정 → cache_read(0.1×)로 재사용. 훌륭한 컨텍스트를 굶기지 말고 캐시하라.

## 출력 통제
- 서론·맺음말·군더더기 제거. **전체 재출력 대신 diff/요점.**
- 끝나면 멈춘다(다음 단계 제안·한 일 요약 금지).
- 최종 답은 짧게, **단 필요한 추론 단계는 확보**(과삭제 금지).
- 구조적 답 필요 시 schema/형식 강제 → 산문 파싱·재시도 왕복 제거.

## 세션 위생
- 작업 경계에서 `/compact`(mid-task 금지 — 캐시 재구성). 무관한 새 작업은 `/clear` 후 새 세션.
- 컨텍스트가 커지고 남은 작업이 작으면 새 세션이 저렴.

## 검증 (rework 방지)
- 완료/커밋 선언 전 **가장 값싼 오라클 먼저**: compile·lint·test·schema.
- 어려운 추론은 적대적 자기점검 1회("틀린 곳을 찾아라"). refine 루프는 2회 상한.

## 도구·스킬 위생
- 안 쓰는 MCP·스킬은 정리(설명이 매 턴 컨텍스트에 상주). 스킬 본문은 타이트하게.
- 반복·복잡·표준 작업 → 스킬로 상각. 일회성·사소 → 그냥 프롬프트.
- 결정론 스크립트·grep로 될 일에 LLM 쓰지 않기(AI-YAGNI).

## Hook 미지원 환경 자가 점검 (Desktop 등)
> 이 섹션은 전역 Skill `token-saver:rules`로 이전됐다(2026-08-05,
> `skills/rules/SKILL.md`) — 플러그인을 설치한 모든 프로젝트에 적용되므로
> 이 repo 로컬 사본은 더 유지하지 않는다. hooks 미지원 환경에서 적용할
> 규칙은 그 Skill 참고. **정정(2026-08-06)**: "Desktop Code 탭=hooks 원천 불가"는
> 과일반화였다 — 실사용 macOS 세션에서 hooks 정상 발화 실측 확인, 원 이슈는 Windows
> 한정. 배경·실측 결과는 `experiments/PROTOCOL.md` 실험11,
> `docs/superpowers/specs/2026-08-05-desktop-active-measurement-design.md` 참고.
