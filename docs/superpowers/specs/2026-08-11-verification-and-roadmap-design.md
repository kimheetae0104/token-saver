# 검증 → 로드맵 설계 (2026-08-11)

## 배경
18차 세션(HANDOFF.md)까지 실험21~23과 문서 승격이 끝난 상태에서, 사용자가 "계획 세우고
로드맵 세워서 토큰 세이버 기능 강화 및 다른 아이디어들로 더 디벨롭할 수 있는 것이
있는지, 아니면 지금까지 결과물로 테스트 검증해보든가 해봐"라고 요청. 브레인스토밍으로
범위를 좁힌 결과, **B(기존 결과물 검증)를 먼저 하고 A(기능 강화·신규 아이디어)는 그
다음**으로 순서를 확정했다. A는 이번 세션에서 "아이디어 목록+우선순위"까지만 산출하고
실제 구현은 다음 세션으로 미룬다.

## 목적
1. 지금까지 만든 8개 hooks·MCP 서버·measure.py·문서(README/HANDOFF/TOKEN-GUIDE/PROTOCOL)가
   실제로 정확한지, 서로 정합적인지, 실사용에서 효과가 있는지 이번 세션 안에서 재확인하고
   발견된 문제는 바로 고친다.
2. 그 결과를 근거로 다음에 개발할 만한 기능·개선 아이디어를 우선순위와 함께 정리해
   다음 세션이 바로 착수할 수 있게 한다.

## 범위
- **포함**: 테스트 스위트 재실행, `claude plugin validate`, 문서-코드 정합성 감사(README/
  HANDOFF/TOKEN-GUIDE/PROTOCOL 대상), `measure.py`를 통한 최근 세션 재측정, 발견된 문제의
  즉시 수정, 아이디어 목록+우선순위 산출.
- **제외**: Phase 2에서 나온 아이디어의 실제 구현(다음 세션), 블로그 관련 요청(다른
  프로젝트로 확인됨, 이 스펙과 무관), 대규모 멀티에이전트 Workflow 오케스트레이션(사용자가
  명시 요청 안 함 — 이번 범위는 일반 서브에이전트 위임까지만).

## Phase 1 — 검증 (순차: 코드건강성 → 정합성 → 실사용효과)

### 1-1. 코드 건강성
방법: 직접 실행(결정론적 명령이라 위임 불필요).
- `pytest`(또는 프로젝트가 실제 쓰는 러너) 전체 스위트 — 현재 문서 주장(215/215) 재확인.
- `claude plugin validate`.
- `hooks/hooks.json` 배선과 실제 파일 존재 여부 대조(누락·오타 배선 탐지).

실패 발견 시: 이번 세션에서 즉시 수정(rework 방지 원칙 — 검증 단계에서 발견된 회귀는
다음 단계로 넘기지 않는다).

### 1-2. 문서 vs 코드 정합성 감사
방법: 문서당 서브에이전트 1개 병렬 위임(대량 스윕 성격, 결론만 회수).
- 대상: `README.md`/`README.en.md`, `HANDOFF.md`, `docs/TOKEN-GUIDE.md`,
  `experiments/PROTOCOL.md`.
- 각 문서의 구체적 주장(수치·임계값·"N/N 통과"·버전 배지·"현재 적용됨" 같은 상태 서술)을
  추출해 현재 코드(`hooks/*.py`의 THRESH 상수, `config_store.py` DEFAULTS, 실제 테스트
  카운트, `plugin.json`/`marketplace.json` 버전)와 대조, 불일치 목록만 반환.
- 우선 확인 대상(이미 알려진 위험 지점): `many_agents=12`(outlier 1건 기반, 세션 누적
  대기 상태로 문서화됨 — 재보정 여부 판단), README 버전배지/테스트뱃지, TOKEN-GUIDE §4
  승격 규칙 표현이 실제 훅 코드(`prompt_gate.py` 등) 동작과 일치하는지.

발견된 불일치는 이번 세션에서 바로 정정(문서 수정 or 코드 수정, 어느 쪽이 진실인지
판단 후).

### 1-3. 실사용 효과 재측정
방법: 직접 `measure.py --all --autopsy` 실행(기존 도구 재사용, 위임 불필요).
- 최근 세션(18차 이후 누적분 포함) 대상으로 hook 실제 발화 빈도, 에스컬레이션률,
  ladder_gate 추천/실제 티어 일치율 등 문서가 인용하는 수치를 재계산.
- 표본이 여전히 작아 결론이 안 바뀌면 "표본 부족, 판단 보류"로 정직히 기록(기존 HANDOFF
  관행과 동일 — 억지로 결론 내지 않음).

### Phase 1 산출물
- 회귀 수정, 문서-코드 불일치 정정, 수치 갱신 — 별도 리포트 파일을 새로 만들지 않고
  실제 파일(코드/문서)을 직접 고치는 방식.
- 큰 발견(반증된 임계값, 재현된 결함 등)은 HANDOFF.md에 새 세션 항목(19차)으로 기록.

## Phase 2 — 개발 아이디어 목록 + 우선순위

### 소싱 3갈래
1. **Phase 1 실측 근거**: 검증 중 실제로 드러난 갭(문서-코드 불일치, 재현된 결함, 표본
   부족으로 판단 보류된 항목) — 가장 신뢰도 높은 소스.
2. **HANDOFF.md 기존 열린 스레드**: many_agents 재보정, verify_fails 재검증, 병렬 haiku
   겹침 등 — Phase 1-3 재측정으로 상태가 바뀌었으면 갱신, 안 바뀌었으면 "여전히 데이터
   대기"로 유지.
3. **신규 가설 아이디어**: 아직 코드에 없는 것(다른 라우팅 신호, MCP 툴 확장, Desktop/
   Windows 격차 완화 등) — 실측 근거가 없으므로 "가설" 딱지를 붙여 1·2와 구분.

### 항목 기록 형식
설명(1줄) · 가치(왜 필요) · 예상 비용(구현 난이도) · 블로커 유무 · 소스(1/2/3).

### 우선순위 산식
가치/비용 비율을 기본으로 하되, 프로젝트 자체 원칙(AI-YAGNI, 오라클 없는 소표본은 실측
금지)에 위배되는 항목은 "제외 사유"와 함께 리스트에서 뺀다 — 뺀 이유도 남겨야 다음 세션이
같은 아이디어를 다시 꺼내지 않는다.

### Phase 2 산출물
- 이 스펙 문서 하단(또는 별도 섹션)에 우선순위 표로 정리.
- `HANDOFF.md` "열린 스레드" 섹션을 이 표 기준으로 갱신 — 구현은 하지 않고, 다음 세션이
  표에서 골라 시작할 수 있는 상태로만 만든다.

## 성공 기준
- Phase 1: 테스트 스위트 결과가 문서 주장과 일치하거나, 불일치가 전부 정정됨.
- Phase 1: 감사 대상 4개 문서에서 발견된 불일치가 전부 처리(수정 또는 "의도적 차이"로
  명시)됨.
- Phase 2: 우선순위 표가 존재하고, 각 항목에 소스·가치·비용·블로커가 채워짐.

## 리스크·오픈 이슈
- 1-3(실사용 재측정)은 세션 누적량에 따라 "판단 보류"로 끝날 수 있음 — 이는 실패가
  아니라 프로젝트의 "실측만 기록" 원칙에 따른 정상적 결과.
- 문서-코드 불일치가 발견됐을 때 "어느 쪽이 진실인지"가 항상 명확하진 않을 수 있음 —
  불명확하면 코드 실행 결과를 우선 진실로 삼는다(문서는 코드를 서술하는 것이 원칙).

## Phase 2 결과 — 아이디어 우선순위 (2026-08-11)

### Task3 "판단 보류"를 뒤집은 발견: ladder_gate 실사용 로깅은 버그로 죽어 있었다

Task3 보고서는 `ladder_gate_events/`가 N=0이라는 것만 확인하고 "위임이 활발한 세션
축적 대기"로 판단 보류했다. 이 태스크(Task5)에서 브리프 지시대로 `hooks/ladder_gate.py`와
`measure.py`를 직접 열어 조사한 결과, 이건 표본 부족이 아니라 **재현 가능한 파싱 버그**다.

- 실제 플러그인 데이터 디렉터리는 `~/.claude/plugins/data/token-saver-*/ladder_gate/`
  글롭에 매치되는 두 곳이다: `token-saver-token-saver-tools/ladder_gate/`(세션당 상태
  파일 106개)와 `token-saver-inline/ladder_gate/`(6개, 그중 1개는 `consulted: true`).
  두 디렉터리 합쳐 `consulted: true`가 총 3개(게이트 자체는 정상 작동 — 컨설트 전엔
  확실히 막힘)지만 **파일 전부(106+6=112개) `recommended_tier` 필드가 없다** — 두
  디렉터리 모두 같은 패턴이라 근거가 더 강해진다.
- 이번 세션(`694f2287-...`) 자신의 트랜스크립트에서 `token_saver_suggest_tier` MCP 툴의
  실제 응답을 직접 확인: `content`가 `{"type":"text","text":"추천: haiku(...) — ..."}`를
  담은 **리스트**로 온다(`[{"type":"text","text":...}]`), dict로 감싸여 오지 않는다.
- 그런데 `hooks/ladder_gate.py`의 `_extract_recommended_tier()`는 PostToolUse payload의
  값이 `str`이거나 `dict`(내부에 `content` 키)인 경우만 처리하고 **`list`는 아예 분기가
  없다** — 재현: `_extract_recommended_tier({"tool_response": [{"type":"text","text":
  "추천: haiku(...) — ..."}]})` → `None`. 반대로 `tests/test_ladder_gate.py`가 검증하는
  `{"content": [...]}` dict-wrapped 형태(실제 배선과 다름)에서는 정상 추출된다 — 테스트가
  코드 자신의 가정을 검증할 뿐, 실제 하네스 payload 형태를 검증하지 못하고 있었다.
- 파급: `recommended_tier`가 한 번도 안 채워지니 `ladder_gate.py`의 "추천/실제 불일치
  1회 재확인"(2026-08-11 강화, 커밋 `1b8668d`)과 "실사용 로깅"(커밋 `3ec5460`/`5d475a4`)
  **두 강화 기능 모두 프로덕션에서 죽은 코드**였다 — 컨설트를 강제하는 1차 게이트(deny
  until consulted)만 정상 동작. README/CLAUDE.md엔 "추천/실제 티어 일치율" 같은 구체적
  수치는 없지만(`grep -n "일치율\|준수율" README.md CLAUDE.md` 결과 무매치, 확인됨),
  사다리 라우팅의 실사용 준수도를 재는 유일한 데이터 소스가 이 로그라서 이 버그가
  고쳐지고 데이터가 새로 쌓이기 전까진 원리적으로 계산 불가능하다.

### 소싱 2 — HANDOFF 기존 열린 스레드 재확인 (상태 변화 없음)

Task3 재측정(`production_failures.jsonl` 142줄, 계획 시점과 동일·0건 성장)으로는 18차가
"블록됨"으로 남긴 3항목의 전제가 하나도 바뀌지 않았다 — 그대로 유지:
- `verify_fails.py` 이질적 judge 재검증: 실전 위음성 재현 사례 여전히 0건.
- `many_agents` 임계값 재보정: 세션 누적 대기, 신규 outlier 없음.
- 병렬(동시 스폰) haiku 겹침 매칭: 실측 사례 여전히 0건.

### 소싱 3 — 신규 가설 브레인스토밍

- TOKEN-GUIDE/PROTOCOL 실험 번호 인용 정합성 자동 검증(Task2 오류 재발 방지, 결정론
  grep 스크립트로 AI-YAGNI 준수).
- `production_failures.jsonl` 142줄 정체(2026-08-08 이후 신규 0건)가 "근본원인이 실제로
  해소됐다"인지 "탐지 로직이 새 실패 패턴을 못 잡고 있다"인지 코드 리뷰로 구분.
- `token_saver_suggest_tier`의 `batch_size` 자동 추정(호출 직전 대상 개수 카운팅) — 단,
  `ladder_gate.py` 자체 주석이 명시하듯 "자유 서술 태스크의 자동 분류"는 실험9에서
  위양성·위음성이 실측된 함정이라 신중 표시.
- Desktop/Windows hooks 격차 완화 — 브레인스토밍만, 검증 불가능해 제외(아래 참고).

### 우선순위 표

| 순위 | 항목 | 설명 | 가치 | 비용 | 블로커 | 소스 |
|---|---|---|---|---|---|---|
| 1 | ladder_gate 추천 티어 파싱 버그 수정 | `_extract_recommended_tier()`에 `list`(실제 MCP PostToolUse payload 형태) 분기 추가 — 재현 코드 확보, 실제 payload 형태로 회귀테스트 교체/추가 | 높음 — 죽어있던 "불일치 재확인" + "실사용 로깅" 두 기능을 살려, README/CLAUDE.md엔 없는 수치이지만 사다리 라우팅 실사용 준수도를 재는 유일한 데이터 소스인 이 로그를 처음으로 실측 가능하게 함 | 낮음 — 함수 하나에 분기 추가 수준(~10줄), 기존 106개 상태파일 데이터엔 영향 없음(신규 세션부터 정상 기록) | 없음, 바로 착수 가능 | 1(Task3 판단보류 항목, 이번 조사로 확인된 버그로 격상) |
| 2 | 문서 인용 정합성 자동 검증 스크립트 | TOKEN-GUIDE.md/README 등이 "실험N"을 인용할 때 PROTOCOL.md에 그 번호가 실제 존재하는지 grep 기반으로 결정론 검사(pytest 또는 CI 스텝) | 중간 — Task2가 발견한 오귀속 2건 같은 재발을 자동 차단 | 낮음 — LLM 없이 grep/스크립트만(AI-YAGNI 준수) | 없음 | 2(Task2 후속), 3 |
| 3 | production_failures 정체 원인 코드 리뷰 | `capture_failures()` 탐지 로직이 2026-08-08 근본원인 수정 이후 실제로 새 실패를 안 만나는 건지, 아니면 새 패턴을 놓치고 있는 사각지대가 있는지 정적으로 검토 | 중간 — 측정 파이프라인 자체의 건강성 확인 | 중간 — 로직을 꼼꼼히 다시 읽고 커버리지 갭을 추론해야 함 | 없음(코드 리뷰만, 실측 불필요) | 1, 3 |
| 4 | `suggest_tier` batch_size 자동 추정 | Agent 호출 직전 컨텍스트에서 위임 대상 개수를 세어 `batch_size` 파라미터를 자동 채움(왕복 축소) | 낮음~중간 — 이미 수동 입력이 저비용이라 절감 폭 작음 | 중간 — "무엇을 셀지" 판단이 프로즈 분류로 새는 순간 실험9 함정 재현 위험, 신중 설계 필요 | 설계 확정 전까지 보류 | 3(가설) |
| 5 | `many_agents` 임계값 재보정 | 신규 outlier 세션 누적 시 임계값(현재 12) 재검토 | 낮음(지금은) | 낮음(지금은 액션 자체 불가) | 세션 자연 누적 대기 — 액션 불가 | 2 |
| 6 | `verify_fails.py` 이질적 judge 재검증 | 실전 위음성 사례가 다시 나오면 judge 경로 재사용해 재검증 | 낮음(지금은) | 낮음(지금은 액션 자체 불가) | 실전 위음성 재현 사례 없음 — 액션 불가 | 2 |
| 7 | 병렬(동시 스폰) haiku 겹침 매칭 처방 | 두 번째 haiku가 첫 번째 종료 전에 시작하는 케이스의 중복 매칭 방지 | 낮음(지금은) | 낮음(지금은 액션 자체 불가) | 실측 사례 없음 — 액션 불가 | 2 |

### 제외
- Desktop/Windows Code 탭 hooks 미발화 완화 — 검증할 Windows 실행 환경이 없어 "오라클
  없는 소표본 실측 금지" 원칙에 걸림(고쳤다고 주장해도 실측으로 확인 불가). 사용자가
  Windows 환경을 제공하기 전까지 착수 보류.
- `ladder_gate` 이벤트에 "이걸 Sonnet으로 돌렸으면 얼마였을까" 식 반증적 $ 환산 추가 —
  `hooks/ladder_gate.py` 자체 설계 원칙이 이미 명시적으로 거부(RTK 허수 counterfactual
  함정, README "시장 비교" 참고). 신규 아이디어가 아니라 기존에 이미 기각된 설계라 재론
  대상 아님.
